export enum EClaimsType {
  RECEIVED = 'received',
  ISSUED = 'issued',
}

export enum EClaimSortOptions {
  ISSUE_DATE = 'Issue Date',
  EXPIRY_DATE = 'Expiry Date',
}

export enum EScopeSortOptions {
  TICKER = 'Scope - Ticker',
  IDENTITY = 'Scope - Identity',
  CUSTOM = 'Scope - Custom',
  NO_SCOPE = 'Scope - None',
}
