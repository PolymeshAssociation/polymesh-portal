import styled from 'styled-components';

export const StyledAssetManager = styled.div`
  padding: 24px;
  background-color: ${({ theme }) => theme.colors.dashboardBackground};
  min-height: 100vh;
`;
