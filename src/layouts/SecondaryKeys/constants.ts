export enum EPermissionType {
  WHOLE = 'Whole',
  THESE = 'These',
  EXCEPT = 'Except',
  NONE = 'None',
}

export enum EPermissionStep {
  SET_PERMISSIONS = 1,
  REVIEW = 2,
}

export enum EPermissionTab {
  ASSETS = 'assets',
  EXTRINSICS = 'extrinsics',
  PORTFOLIOS = 'portfolios',
}
