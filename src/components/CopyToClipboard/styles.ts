import styled from 'styled-components';

export const StyledCopyWrapper = styled.div`
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;

  & .check-icon {
    color: #00aa5e;
  }
`;
