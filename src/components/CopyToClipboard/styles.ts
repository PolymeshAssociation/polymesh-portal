import styled from 'styled-components';

export const StyledCopyWrapper = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;

  & .check-icon {
    width: 16px;
    height: 16px;
    color: #00aa5e;
    transform: scale(1.1);
  }
`;
